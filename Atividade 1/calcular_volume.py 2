raio = float(input("informe o raio:"))
altura = float(input("informe a altura: "))
volume =((RAIO**2)*ALTURA*3.14159)

# Calcula o volume
volume = calcular_volume_lata(raio*altura)

 # Exibe o resultado
Print(f"O volum da lata é: {volume:.2f}")#exibir resultado